# CacheConfig (which owns the RedissonClient bean) is disabled under this profile, so the
# Redisson starter would otherwise auto-configure its own client - and unlike Lettuce it
# connects eagerly at startup, failing the context when no Redis server is running.
spring.autoconfigure.exclude=org.redisson.spring.starter.RedissonAutoConfigurationV4